// import { mount } from "cypress/vue";
import "../../assets/css/main.css"; // Import your global styles if needed

// Global before hook to run before each test
beforeEach(() => {
  // Add any global setup here
});
